apiVersion: v1
kind: Service
metadata:
  labels:
    app: accounts
  name: accounts
spec:
  type: NodePort
  selector:
    app: accounts
  ports:
    - name: http
      port: 8080
      protocol: TCP
      targetPort: 8080
